button(class='toggle_visible create_post colorize_texture_#{bubble_section}')
  block button_text

form.toggle_invisible.add_event.form-horizontal(method="post", action="/bubbles/#{bubble._id}/#{bubble_section}/create")
  block form
