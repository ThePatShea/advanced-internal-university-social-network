extends ./sidebar_top_bubble_template

block bubble_attributes
  h1= bubble.name
  include ../includes/subscribe
  - if (typeof(bubble.creator) != 'undefined' && bubble.creator.equals(req.user._id) )
    include ../includes/edit_bubble
