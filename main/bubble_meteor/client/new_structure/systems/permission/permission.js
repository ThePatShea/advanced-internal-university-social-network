/*
Handlebars.registerHelper("systemPermission", {
    bubble : {
        create : {
            canMakeSuperBubble : [isLevel(2), isLevel(4)]
        }
    }
});


var checkPermission = {
    isLevel : function(level) {
      if (Meteor.user().userType == level)
        return true;
      else
        return false;
    }
}
*/
