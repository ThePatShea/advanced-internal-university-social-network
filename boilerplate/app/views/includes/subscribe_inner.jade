input(type='hidden', name='current_url', value=req.url)
button.subscribe(type='submit')
  - if (user_subscribed == 1)
    div.heart.icon_subscribed
  - else
    div.heart.icon_unsubscribed
  div.descriptor.gray_text= bubble.num_subscriptions
