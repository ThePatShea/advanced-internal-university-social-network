//created by Shao Qi

Users = new Meteor.Collection('users');

Meteor.methods({
	user: function(userAttributes){
		//var user = Meteor.user(),
		//	bubbleWithSameName = Bubbles.findOne({title:bubbleAttributes.title});

    var user = _.extend(_.pick(userAttributes, 'name', 'emailAccount'), {
    	submitted: new Date().getTime(),
      lastUpdated: new Date().getTime(),
      users: {
        applicants: [],
        invitees: [],
        members: [],
        admins: [user._id]
      }
    });

    return userId;
	}

});
