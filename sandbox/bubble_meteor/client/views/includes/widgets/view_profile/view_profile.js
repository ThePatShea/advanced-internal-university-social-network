Template.viewProfile.events({
    'click .view-profile': function(event) {
      // Disable the parent button
        // event.stopPropagation();

      // Route to the post page and scroll down to the comments div
        // Meteor.Router.to('/mybubbles/'+this.bubbleId+''+'/posts/'+this._id+'#comments');
    }
});
